header.header 
   nav.nav.nav--tours 
      a.nav__el(href="/") All tours 
   .header__logo 
      img(src="/assets/img/people.svg" alt="Tour-Guide logo")  
   nav.nav.nav--user 
      if user 
         a.nav__el.nav__el--logout Log Out 
         a.nav__el(href="#") 
            img.nav__user-img(src=`/assets/img/users/${user.photo}` alt=`Photo ${user.name}`)
            span= user.name.split(' ')[0]
      else 
         a.nav__el(href='/login') Log in 
         a.nav__el.nav__el--cta(href='/signup') Sign Up